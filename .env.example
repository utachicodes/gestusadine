# Convex Deployment
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Admin Configuration
ADMIN_EMAILS=admin@example.com

# Security (set via `npx convex env set` for production)
# AUTH_SECRET=your-random-secret
# FANAR_API_KEY=your-fanar-api-key (get from https://api.fanar.qa)
